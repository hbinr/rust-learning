// 创建和使用数组
fn arrary() {
    //  定义数组

    // 未指定长度的逗号分隔的值列表。
    let nums = [1, 2, 3, 4, 5];

    // 初始值后跟一个分号，然后是数组长度。
    let bytes = [0; 5];

    let one = nums[0];

    // 越界
    // let five = nums[5]
}
